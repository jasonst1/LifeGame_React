import React, {createContext} from 'react';

export const GameTime = createContext(null);
export const UserData = createContext(null);
export const StatsData = createContext(null);
export const HourMinute = createContext(null);
export const WeatherData = createContext(null);
export const PageData = createContext(null);